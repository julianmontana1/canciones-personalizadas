PORT=3001
# Optional: set your own initial Superadmin password here. If left unset, the
# server generates a strong random one on first boot and prints it once to the logs.
# ADMIN_PASSWORD=
ELEVENLABS_API_KEY=tu_api_key_aqui
